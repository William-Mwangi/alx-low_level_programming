#include <stdio.h>
#include <stdlib.h>

/**
 * main - check code
 * @argc: argc
 * @argv: argv
 * Return: int
 */

int main(int argc, char *argv[])
{
	if (argc > 0)
	{
		printf("%s", argv[0]);
	}
	printf("\n");
	return (0);
}
