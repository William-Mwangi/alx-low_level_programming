#include<stdio.h>
/**
 * main  -prints a statement using the printf function
 * Description: uses the printf function instead of puts function
 * Return: value of zero
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
