#include<stdio.h>
/**
 * main  -prints a statement using the puts function
 * Description: uses the puts funtion instead of the printf function
 * Return: value of zero
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
